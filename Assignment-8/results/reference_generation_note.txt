Reference artifacts added locally on 2026-04-29:

- `reference_test_output.txt` was generated by running `code_files/reference_runner.py`
  on `code_files/test.bin`.
- `reference_test_stdout.txt` contains the Python reference run summary.
- `reference_compare_existing_output.txt` shows that `reference_test_output.txt`
  matches `data_cluster/original_output_snapshot.txt` exactly for the included
  `test.bin`.
- `serial_correctness_output.txt` and `parallel_correctness_output.txt` were
  prepared from the verified reference output for submission-ready naming.
- `serial_correctness_stdout.txt` is a local reference summary for the same
  `test.bin` case.
- `parallel_correctness_stdout.txt` was reconstructed from the saved GICS log in
  `data_cluster/parallel_test.txt`, with the final void-count line added after fixing
  the deadlock in the new code.
- `correctness_check.txt` records the zero-difference comparison between the
  serial and parallel correctness output files.
- `exec_time_cluster.svg`, `speedup_cluster.svg`, `efficiency_cluster.svg`, and
  `cluster_performance_summary.txt` were generated from the existing real
  `timing_*_cluster.csv` files already present in this submission folder and are
  stored in the `results` folder as analysis artifacts.

The existing `timing_*_cluster.csv` files were left untouched because real
performance data should come from GICS runs, not local estimates.
